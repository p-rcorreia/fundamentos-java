# 02 - Tipos primitivos vs objetos

## 🎯 Objetivo

Entender a diferença entre valores primitivos, wrappers, referências e objetos em Java.

## ✅ Checklist

- [ ] `int`, `long`, `double`, `boolean`, `char`
- [ ] Wrappers: `Integer`, `Long`, `Double`, `Boolean`
- [ ] Autoboxing e unboxing
- [ ] `null` em objetos
- [ ] Comparação com `==` e `equals`
- [ ] Diferença entre valor e referência
- [ ] Imutabilidade em `String`
- [ ] `BigDecimal` para valores monetários

## 💡 Por que importa no backend?

Erros com `null`, comparação incorreta e valores monetários são comuns em sistemas reais.

## 🧪 Exercício sugerido

Criar exemplos comparando primitivos, wrappers, `String`, `BigDecimal`, `==` e `equals`.

## 📌 Percepções

> Registrar aprendizados conforme o estudo avançar.
